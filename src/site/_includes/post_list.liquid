<ul class="post-list">
  {% for post in posts %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <span class="post-date">{{ post.date | date: "%B %e, %Y" }}</span>
    </li>
  {% endfor %}
</ul>